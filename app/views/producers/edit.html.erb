<% provide(:title, 'Edit Producer') %>
<h1>Edit Producer</h1>

<div class="row">
  <div class="span6 offset=3">
    <%= form_for(@producer) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
