<% provide(:title, 'New Producer') %>
<h1>New Producer</h1>

<div class="row">
  <div class="span6 offset=3">
    <%= form_for(@producer) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create new producer", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
