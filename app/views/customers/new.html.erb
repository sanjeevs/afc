<% provide(:title, 'New Customer') %>
<h1>New Customer</h1>

<div class="row">
  <div class="span6 offset=3">
    <%= form_for(@customer) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create new customer", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
